#!/bin/sh -ex
version="${XBUILD?}"
# write gradle.properties
echo "version=${version?}" > gradle.properties
# gradle
gradle bootJar
# for pip install
sed s/VERSION/${version?}/ setup.py.in > setup.py
zip xbuild-jar.zip setup.py xbuild.jar
